messages = ds_list_create();
message_id = 0;
messageText = "";
enum MSG { TEXT }

// Dimensões
height = 200;
width = 400;

// Deinição manual da hitbox
hitbox_left = x;
hitbox_right = x + width;
hitbox_top = y;
hitbox_bottom = y + height;

padding = 24;

